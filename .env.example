NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

SUPABASE_SERVICE_ROLE_KEY=

B2_KEY_ID=
B2_APPLICATION_KEY=
B2_BUCKET_NAME=
B2_REGION=
B2_ENDPOINT=

# AI Job Intelligence Engine — server-only, never exposed to the client.
# Get a token at https://huggingface.co/settings/tokens (read access is enough).
# If unset, the job parser silently falls back to the deterministic heuristic parser.
HUGGINGFACE_API_KEY=
